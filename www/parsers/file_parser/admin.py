# -*- coding: utf-8 -*-

from django.contrib import admin

from parsers.file_parser.models import Uploader, File

################################################################################################################
################################################################################################################
	
class UploaderAdmin(admin.ModelAdmin):
	list_display = ('title', 'sort')
	list_editable = ('sort',)
		
admin.site.register(Uploader, UploaderAdmin)

################################################################################################################
################################################################################################################
	
#Τΰιλ
class FileAdmin(admin.ModelAdmin):
	list_display = ('file', 'uploader', 'city', 'date_creat', 'date_start', 'date_end', 'count_item', 'status')
		
admin.site.register(File, FileAdmin)
	
################################################################################################################
################################################################################################################